{
  "name": "@gaga-gps/client",
  "version": "0.0.0",
  "private": true,
  "description": "Fetch tipado + cliente de Socket.io tipado + sesión compartida, usados por apps/web-app. Reemplaza los patrones BACKEND_URL/API + fetch sueltos de cada panel.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@gaga-gps/shared-types": "*",
    "socket.io-client": "^4.8.3"
  }
}
