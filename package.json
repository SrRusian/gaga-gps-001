{
  "name": "gaga-gps-001",
  "private": true,
  "version": "1.0.0",
  "description": "GAGA-GPS — monorepo (backend + apps web + paquetes compartidos)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "dev:backend": "npm run dev --workspace=@gaga-gps/backend"
  },
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.0",
    "eslint": "^9.15.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.9.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.14.0",
    "vitest": "^4.1.10"
  }
}
