// Global (todos los proyectos) o el id de un proyecto especifico - compartido por todos los hooks
// de recursos de este panel, cada uno filtra su propia lista segun este valor.
export type Scope = 'global' | number;
