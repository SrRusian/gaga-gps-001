{
  "name": "@gaga-gps/android-operator",
  "version": "0.0.0",
  "private": true,
  "description": "App nativa Android (Capacitor) que unifica Operador web + envio de posicion estilo Traccar Client + receptor RTK/NTRIP con mock-location, en un solo APK.",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "cap:sync": "npm run sync:web && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
