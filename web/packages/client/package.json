{
  "name": "@gaga-gps/client",
  "version": "0.0.0",
  "private": true,
  "description": "Fetch tipado + cliente de Socket.io tipado + sesión compartida, usados por web. Reemplaza los patrones BACKEND_URL/API + fetch sueltos de cada panel.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@capacitor/core": "^7.4.4",
    "@gaga-gps/shared-types": "*",
    "socket.io-client": "^4.8.3"
  }
}
